{
  "name": "matrixflow",
  "version": "0.1.0",
  "description": "MatrixFlow — 多平台内容分发桌面应用",
  "main": "dist/electron/main.js",
  "scripts": {
    "dev": "concurrently \"vite\" \"wait-on http://localhost:5173 && npm run build:electron && electron .\"",
    "dev:vite": "vite",
    "dev:electron": "npm run build:electron && electron .",
    "build": "npm run build:electron && vite build",
    "build:electron": "tsc -p tsconfig.electron.json",
    "start": "npm run build && electron .",
    "preview": "vite preview",
    "lint": "eslint . --ext .ts,.vue",
    "typecheck": "vue-tsc --noEmit && tsc --noEmit",
    "test": "vitest run tests/unit tests/integration",
    "test:watch": "vitest tests/unit tests/integration",
    "test:coverage": "vitest run tests/unit tests/integration --coverage",
    "test:all": "vitest run",
    "test:e2e": "vitest run tests/e2e",
    "test:e2e:watch": "vitest tests/e2e",
    "test:login": "tsx scripts/test-login.ts",
    "test:publish": "tsx scripts/test-publish.ts",
    "pack": "npm run build && electron-builder --dir",
    "pack:mac": "npm run build && electron-builder --mac",
    "pack:win": "npm run build && electron-builder --win",
    "pack:linux": "npm run build && electron-builder --linux",
    "dist": "npm run build && electron-builder",
    "dist:mac": "npm run build && electron-builder --mac --publish always",
    "dist:win": "npm run build && electron-builder --win --publish always",
    "release": "bash scripts/release.sh"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "echarts": "^6.0.0",
    "electron-log": "^5.1.0",
    "electron-updater": "^6.1.0",
    "element-plus": "^2.5.0",
    "patchright": "^1.0.0",
    "pinia": "^2.1.0",
    "vue": "^3.4.0",
    "vue-echarts": "^8.0.1",
    "vue-router": "^4.3.0",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@electron/notarize": "^2.3.0",
    "@element-plus/icons-vue": "^2.3.2",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "@vitejs/plugin-vue": "^5.0.0",
    "@vitest/coverage-v8": "^4.1.6",
    "concurrently": "^8.2.2",
    "electron": "^41.6.0",
    "electron-builder": "^24.13.0",
    "happy-dom": "^20.9.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^5.4.0",
    "vitest": "^3.0.0",
    "vue-tsc": "^2.0.0",
    "wait-on": "^7.2.0"
  }
}
